import React from 'react';
import { FlexableSpaceDiv } from './FlexableSpace.styled';


//used to add space between layoutting component
const FlexableSpace = () =>{

    return (

        <FlexableSpaceDiv />
    );
}

export default FlexableSpace;